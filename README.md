LlamaBucket Server
=========

A server for the LlamaBucket web application

Setup:
===

On terminal:

1 - `git clone https://github.com/cesarcruz91/lb_server.git` 

2 - `npm install`

3 - `node app.js`
